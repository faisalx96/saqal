# Required - API Key for LLM provider
OPENROUTER_API_KEY=sk-or-...

# Optional - Use OpenAI directly instead
# OPENAI_API_KEY=sk-...

# Optional - Database location (defaults to ./data/workbench.db)
# DATABASE_PATH=./data/workbench.db

# Optional - Default model settings
DEFAULT_MODEL=gpt-4o-mini
DEFAULT_TEMPERATURE=0.7

# Optional - Model for GEPA reflection (defaults to DEFAULT_MODEL)
# REFLECTION_MODEL=gpt-4o

# Optional - MLflow tracking URI (defaults to file-based local store in data/mlruns/)
# MLFLOW_TRACKING_URI=file:///path/to/mlruns

# Optional - Embedding model for MemAlign episodic memory (defaults to text-embedding-3-small)
# Note: If using OpenRouter, you may need a separate OPENAI_API_KEY for embeddings
# MEMALIGN_EMBEDDING_MODEL=openai:/text-embedding-3-small

# Optional - Number of similar examples to retrieve for MemAlign episodic memory
# MEMALIGN_RETRIEVAL_K=3
